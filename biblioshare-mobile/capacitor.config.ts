import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.biblioshare.mobile',
  appName: 'biblioshare-mobile',
  webDir: 'www',
};

export default config;
